[Unit]
Description=Oakleon
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker run -d -p 80:3000 -w="/root/oakleon_server/" tim/oakleon /usr/bin/npm start

[Install]
WantedBy=local.target